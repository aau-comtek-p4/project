cmake_minimum_required(VERSION 3.16)

# If you really need custom COMPONENTS, you can keep set(COMPONENTS ...),
# but usually you don't need it at all.
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(blink_cxx)

